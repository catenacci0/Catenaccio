---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import FormattedDate from "../../components/FormattedDate.astro";
import { t, LOCALES } from "../../i18n";
import { getCollection } from 'astro:content';
import { createLangUrl } from "../../utils/urls";

export function getStaticPaths() {
  return LOCALES.map((lang) => ({ params: { lang } }));
}

const { lang } = Astro.params;
const _ = (k) => t(lang, k);

// Get latest articles for this language
const allPosts = await getCollection('blog', ({ data }) => data.lang === lang);
const latestPosts = allPosts
  .sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime())
  .slice(0, 6); // Show latest 6 articles
---

<BaseLayout 
  title={`${_('home.hero.title')} — ${_('common.tagline')}`} 
  description={_('home.hero.description')}
  lang={lang}
>
  <Header lang={lang} />
  
  <!-- Language Switcher -->
  <div class="bg-gray-50 dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 transition-colors duration-300">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-2">
      <div class="flex justify-end space-x-4">
        <a href={createLangUrl('it')} class={`text-sm transition-colors duration-200 ${lang === 'it' ? 'font-bold text-blue-600 dark:text-blue-400' : 'text-gray-600 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400'}`}>
          🇮🇹 Italiano
        </a>
        <a href={createLangUrl('en')} class={`text-sm transition-colors duration-200 ${lang === 'en' ? 'font-bold text-blue-600 dark:text-blue-400' : 'text-gray-600 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400'}`}>
          🇬🇧 English
        </a>
        <a href={createLangUrl('es')} class={`text-sm transition-colors duration-200 ${lang === 'es' ? 'font-bold text-blue-600 dark:text-blue-400' : 'text-gray-600 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400'}`}>
          🇪🇸 Español
        </a>
      </div>
    </div>
  </div>

  <main class="bg-white dark:bg-gray-900 transition-colors duration-300">
    <!-- Hero Section -->
    <section class="bg-gradient-to-br from-green-600 to-blue-600 dark:from-green-700 dark:to-blue-700 text-white py-24 transition-colors duration-300">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <div class="max-w-4xl mx-auto">
          <h1 class="text-4xl md:text-6xl font-bold mb-6 leading-tight">
            {_('home.hero.title')}
          </h1>
          <p class="text-xl md:text-2xl mb-6 text-green-100">
            {_('home.hero.subtitle')}
          </p>
          <p class="text-lg mb-8 text-blue-100 leading-relaxed">
            {_('home.hero.description')}
          </p>
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href={createLangUrl(lang, '/blog')} class="inline-block bg-white text-blue-600 font-semibold px-8 py-3 rounded-lg hover:bg-gray-100 transition-colors">
              {_('nav.blog')}
            </a>
            <a href={createLangUrl(lang, '/about')} class="inline-block border-2 border-white text-white font-semibold px-8 py-3 rounded-lg hover:bg-white hover:text-blue-600 transition-colors">
              {_('nav.about')}
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="py-20 bg-gray-50 dark:bg-gray-800 transition-colors duration-300">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <h2 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-gray-100 mb-4 transition-colors duration-300">
            {_('home.features.title')}
          </h2>
        </div>
        
        <div class="grid md:grid-cols-3 gap-8">
          <!-- Tactical Analysis -->
          <div class="bg-white dark:bg-gray-700 p-8 rounded-lg shadow-md hover:shadow-lg transition-all duration-300">
            <div class="w-12 h-12 bg-green-100 dark:bg-green-900 text-green-600 dark:text-green-400 rounded-lg flex items-center justify-center mb-6 transition-colors duration-300">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 dark:text-gray-100 mb-4 transition-colors duration-300">{_('home.features.tactical.title')}</h3>
            <p class="text-gray-600 dark:text-gray-300 transition-colors duration-300">{_('home.features.tactical.description')}</p>
          </div>

          <!-- Data Analysis -->
          <div class="bg-white dark:bg-gray-700 p-8 rounded-lg shadow-md hover:shadow-lg transition-all duration-300">
            <div class="w-12 h-12 bg-blue-100 dark:bg-blue-900 text-blue-600 dark:text-blue-400 rounded-lg flex items-center justify-center mb-6 transition-colors duration-300">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 8v8m-4-5v5m-4-2v2m-2 4h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 dark:text-gray-100 mb-4 transition-colors duration-300">{_('home.features.data.title')}</h3>
            <p class="text-gray-600 dark:text-gray-300 transition-colors duration-300">{_('home.features.data.description')}</p>
          </div>

          <!-- Multilingual -->
          <div class="bg-white dark:bg-gray-700 p-8 rounded-lg shadow-md hover:shadow-lg transition-all duration-300">
            <div class="w-12 h-12 bg-purple-100 dark:bg-purple-900 text-purple-600 dark:text-purple-400 rounded-lg flex items-center justify-center mb-6 transition-colors duration-300">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5h12M9 3v2m1.048 9.5A18.022 18.022 0 016.412 9m6.088 9h7M11 21l5-10 5 10M12.751 5C11.783 10.77 8.07 15.61 3 18.129"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 dark:text-gray-100 mb-4 transition-colors duration-300">{_('home.features.multilang.title')}</h3>
            <p class="text-gray-600 dark:text-gray-300 transition-colors duration-300">{_('home.features.multilang.description')}</p>
          </div>
        </div>
      </div>
    </section>

    <!-- About Section -->
    <section class="py-20 bg-white dark:bg-gray-900 transition-colors duration-300">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-gray-100 mb-8 transition-colors duration-300">
          {_('home.about.title')}
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-300 leading-relaxed transition-colors duration-300">
          {_('home.about.description')}
        </p>
        <div class="mt-12">
          <a 
            href={createLangUrl(lang, '/about')}
            class="inline-block bg-blue-600 dark:bg-blue-700 text-white font-semibold px-8 py-3 rounded-lg hover:bg-blue-700 dark:hover:bg-blue-600 transition-colors duration-200"
          >
            {_('common.readMore')}
          </a>
        </div>
      </div>
    </section>

    <!-- Latest Articles Preview -->
    <section class="py-20 bg-gray-50 dark:bg-gray-800 transition-colors duration-300">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <h2 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-gray-100 mb-4 transition-colors duration-300">
            {_('home.latestNews.title')}
          </h2>
          <p class="text-lg text-gray-600 dark:text-gray-300 transition-colors duration-300">
            {lang === 'it' ? 'Le nostre ultime analisi e approfondimenti' : 
             lang === 'es' ? 'Nuestros últimos análisis y perspectivas' :
             'Our latest analysis and insights'}
          </p>
        </div>
        
        {latestPosts.length > 0 ? (
          <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12">
            {latestPosts.map((post) => (
              <article class="bg-white dark:bg-gray-700 rounded-lg shadow-md hover:shadow-lg transition-all duration-300 overflow-hidden">
                <div class="p-6">
                  <div class="flex items-center text-sm text-gray-500 dark:text-gray-400 mb-3 transition-colors duration-300">
                    <FormattedDate date={post.data.date} />
                  </div>
                  <h3 class="text-xl font-semibold mb-3 line-clamp-2">
                    <a href={createLangUrl(lang, `/blog/${post.data.id || post.slug}`)} class="text-gray-900 dark:text-gray-100 hover:text-blue-600 dark:hover:text-blue-400 transition-colors duration-200">
                      {post.data.title}
                    </a>
                  </h3>
                  {post.data.description && (
                    <p class="text-gray-600 dark:text-gray-300 mb-4 line-clamp-3 transition-colors duration-300">
                      {post.data.description}
                    </p>
                  )}
                  <a 
                    href={createLangUrl(lang, `/blog/${post.data.id || post.slug}`)}
                    class="inline-flex items-center text-blue-600 dark:text-blue-400 hover:text-blue-700 dark:hover:text-blue-300 font-medium transition-colors duration-200"
                  >
                    {_('common.readMore')}
                    <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                  </a>
                </div>
              </article>
            ))}
          </div>
        ) : (
          <div class="text-center py-12">
            <p class="text-gray-500 dark:text-gray-400 text-lg transition-colors duration-300">{_('home.latestNews.noArticles')}</p>
          </div>
        )}
        
        <div class="text-center">
          <a 
            href={createLangUrl(lang, '/blog')}
            class="inline-block bg-green-600 dark:bg-green-700 text-white font-semibold px-8 py-3 rounded-lg hover:bg-green-700 dark:hover:bg-green-600 transition-colors duration-200"
          >
            {_('common.seeMore')}
          </a>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>


